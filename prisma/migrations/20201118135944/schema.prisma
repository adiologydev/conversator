generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model guild {
  id      String   @unique @id
  prefix  String
  channel String?
  enabled Boolean?
}
